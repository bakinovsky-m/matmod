#include "header.h"

int main(){
	// Field f;
    double deltaT = 0.1;
    double deltaX = 0.1;
    double maxT = 1.8;
    double maxX = 1.8;
    bool flag = 0;
    init(deltaT, deltaX, maxT, maxX, flag);
	return 0;
}